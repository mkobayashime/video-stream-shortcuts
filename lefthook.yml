pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: bunx oxfmt {staged_files} && bunx oxlint --fix --type-aware {staged_files}
      stage_fixed: true
